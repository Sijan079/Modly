# TODO

## Updates Page

- Add an Updates page that checks downloaded mods, resource packs, and shader packs for newer versions on Modrinth and CurseForge.
  - Match local files by saved metadata, project URL, project ID/slug, filename fallback, and loader/Minecraft version where available.
  - Show current version, latest compatible version, source, release date, and update status.
  - Support separate filters for mods, resource packs, shader packs, up-to-date items, unknown matches, and update available.
  - Keep this as a planner/catalog feature: report update availability and source links, but do not turn the app into a launcher.
